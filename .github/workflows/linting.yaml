name: Linting Pipeline

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16' # You can change this to a compatible Node.js version

      - name: Install Dependencies
        run: npm install --save-dev htmlhint stylelint stylelint-config-standard

      - name: Grant Execute Permissions for Stylelint
        run: chmod +x ./node_modules/.bin/stylelint

      - name: Run HTML Lint
        run: npx htmlhint "**/*.html"

      - name: Run CSS Lint
        run: npx stylelint "**/*.css"
